<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Disable cleartext (HTTP) traffic by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for development and production server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">89.221.212.145</domain>
    </domain-config>

    <!--
    Production certificate pinning - uncomment and configure when deploying
    To get the pin, use:
    openssl s_client -servername YOUR_DOMAIN -connect YOUR_DOMAIN:443 | openssl x509 -pubkey -noout | openssl rsa -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64

    <domain-config>
        <domain includeSubdomains="true">your-production-url.com</domain>
        <pin-set expiration="2025-12-31">
            <pin digest="SHA-256">YOUR_PRIMARY_PIN_HERE</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_HERE</pin>
        </pin-set>
    </domain-config>
    -->

    <!-- Debug builds can trust user-installed certificates -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
