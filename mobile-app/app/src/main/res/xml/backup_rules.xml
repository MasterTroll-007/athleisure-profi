<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude all shared preferences including encrypted ones -->
    <exclude domain="sharedpref" path="."/>
    <!-- Exclude datastore files -->
    <exclude domain="file" path="datastore/"/>
</full-backup-content>
